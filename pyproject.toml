[project]
name = "decky-trailer-tv"
version = "0.1.0"
description = "Trailer TV Decky plugin backend"
requires-python = ">=3.11"

[dependency-groups]
dev = ["pytest>=8.0", "pytest-cov>=5.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
